'use strict';

module.exports = {
  up: async (queryInterface, Sequelize) => {
    // Seeder vacío - los datos iniciales están en las migraciones
  },

  down: async (queryInterface, Sequelize) => {
    // Seeder vacío
  }
};
